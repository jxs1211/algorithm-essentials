package algorithm

/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */
func findMode(root *TreeNode) []int {
	res := []int{}
	// m := make(map[int]int)
	// traverse(root, m)
	// start := 0
	// for k, v := range m {
	// 	start = max(start, v)
	// }
	return res
}
